# GEO-SOP 安装与使用

## 安装

从 [geo.allgood.cn](https://geo.allgood.cn/) 下载对应系统的长期链接安装包：

- Windows 10/11 64 位：运行 `GEO-SOP-Setup-dev.exe`。安装包已经包含运行环境和 Chromium，不需要安装 Python。
- Apple Silicon Mac：下载 Apple Silicon DMG。
- Intel Mac：下载 Intel DMG。

macOS DMG 中将 `GEO-SOP.app` 拖入 `Applications` 后再从“应用程序”打开。正式 V1.0 只有在完成对应平台签名后才会标记为稳定商业版。

## 首次使用

1. 使用与 `geo.allgood.cn` 相同的账号登录；没有账号时先在官网注册。
2. 在“检测平台登录”中选择平台，并在本机浏览器完成登录。
3. 创建监测任务，填写品牌词、问题和需要采集的平台。
4. 开始采集后，可在任务卡片看到完成、部分完成或失败状态及原因。
5. 在数据看板查看品牌曝光、引用来源、趋势、GEO 稿件引用和舆情结果。
6. 使用“上传云端”同步全部统计与现有截图，不需要手动选择文件。

## 数据与同步

本地 SQLite 是桌面端主数据，平台 Cookie 和浏览器 Profile 只保存在本机。客户端通过 HTTPS API 与云端同步，不持有 MySQL 权限。正常升级或重装不会主动删除本地历史；登录同一云端账号后也会合并恢复已同步记录。

## 导出位置

桌面端导出的 Excel、截图 ZIP 和长图默认保存到当前用户的：

```text
Downloads/GEO-SOP/
```

界面会显示实际保存路径。文件重名时会自动增加序号，不会覆盖旧文件。

## 常见问题

- **平台显示未登录：** 点击对应平台“重新检测”；若仍失败，在本机重新登录该平台。
- **任务部分完成：** 查看任务卡片中的首个失败原因，处理登录失效、页面变化、网络或超时后重新执行。
- **云端任务一直等待：** 确认同一账号的桌面客户端已打开，顶部云端状态显示在线。
- **同步暂时失败：** 客户端会自动重试，也可以点击“立即重连”或“上传云端”。
- **看不到截图：** 先确认本地截图存在，再执行一键上传；云端详情会显示已上传的截图。

仓库里的 `.bat`、`.ps1` 和 Python 启动方式仅供开发与故障恢复，不是客户安装流程。项目不提供默认管理员账号或默认密码。
